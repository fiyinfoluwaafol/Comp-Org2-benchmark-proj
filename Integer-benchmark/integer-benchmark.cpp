#include <chrono>
#include <cmath>
#include <iostream>
using namespace std;
using namespace std::chrono;

void Calculations(){
    int result; // Stores result of the current calculation
    
    for (int i = 0; i < 1000000000; i++){ // Loop for the 10^9 iterations
        result = 1 + 1; // Addition of integer constants
    }
    
}